# Structure

Where new code goes, and what it must be named. Every rule here is enforced
by `eslint-plugin-check-file` in [`eslint.config.mjs`](eslint.config.mjs) —
this document hand-mirrors that config rather than being generated from
it, so if they ever disagree, the config wins and this file is stale.
`pnpm lint` fails on a misnamed file in a governed directory.

## Governed directories (filename suffix enforced)

| Directory              | Required suffix   | Example                               | Status in this repo                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ---------------------- | ----------------- | ------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/controllers/`     | `*.controller.ts` | `user.controller.ts`                  | 9 controllers, each a `BaseController` subclass with arrow-field handlers, exported as one singleton instance (`export const tenantController = new TenantController()`); `base.controller.ts` (`BaseController`, `Handler`), `helpers.controller.ts` (`authenticatedUserId`, `actorFrom`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `src/repositories/`    | `*.repository.ts` | `user.repository.ts`                  | `base.repository.ts` (abstract) plus 12 other classes. 3 extend it (`user.repository.ts`, `user-token.repository.ts`, `tenant.repository.ts`); every public method it defines, including these inherited ones, takes a final optional `executor: DbExecutor = db` parameter. The other 9 (`email-log.repository.ts`, `auth-provider.repository.ts`, `notification.repository.ts`, `notification-preference.repository.ts`, `tenant-settings.repository.ts`, `tenant-invitation.repository.ts`, `user-membership.repository.ts`, `audit-log.repository.ts` (insert and keyset list only), `platform-tenant.repository.ts` (staff search; imported only by `services/platform-*.service.ts`)) are standalone — deliberately NOT extending it — each has no soft-delete concept on its table, per its own header comment |
| `src/services/`        | `*.service.ts`    | `database.service.ts`                 | `database.service.ts`, `redis.service.ts`, `mailer.service.ts`, `auth.service.ts`, `google-auth.service.ts`, `session.service.ts`, `verification.service.ts`, `tenant.service.ts`, `profile.service.ts`, `notification.service.ts`, `tenant-membership.service.ts`, `tenant-invitation.service.ts`, `request-context.service.ts`, `tenant-access.service.ts`, `platform.service.ts`, `platform-tenant.service.ts`, `audit.service.ts`, plus `lifecycle.service.ts`, `logger.service.ts`, `notification-emitter.service.ts`, `queue.service.ts`, `session-denylist.service.ts` and `worker-supervisor.service.ts`                                                                                                                                                                                                      |
| `src/policies/`        | `*.policy.ts`     | `tenant.policy.ts`                    | `tenant.policy.ts` — pure, boolean-returning, never throws                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `src/presenters/`      | `*.presenter.ts`  | `user.presenter.ts`                   | `user.presenter.ts` (`toAuthenticatedUser`, `toPublicUser`, `toProfileResponse`), `auth-provider.presenter.ts`, `tenant.presenter.ts` (`toTenantListRow`, `toTenantDetail`), `audit.presenter.ts` (`toAuditEntry`, `toPlatformAuditEntry`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `src/errors/`          | none — see below  | `http-error.ts`, `postgres-errors.ts` | `HttpError`, `isUniqueViolation`, `redactedForLog`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `src/validators/`      | `*.validators.ts` | `user.validators.ts`                  | `auth.validators.ts`, `profile.validators.ts`, `tenant.validators.ts`, `invitation.validators.ts`, `notification.validators.ts`, `verification.validators.ts`, `platform.validators.ts`, `audit.validators.ts`, `cursor.validators.ts` (`cursorField`); `parseBody` lives in `src/validators/parse.validators.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `src/routes/`          | `*.routes.ts`     | `user.routes.ts`                      | `index.routes.ts` (builds and mounts the versioned `/api/v1` router — a router, not a re-export; see "No barrel files" below), `auth.routes.ts`, `profile.routes.ts`, `tenant.routes.ts`, `invitation.routes.ts`, `notification.routes.ts`, `platform.routes.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `src/middlewares/`     | `*.middleware.ts` | `error.middleware.ts`                 | `error.middleware.ts` (`errorHandler`; `HttpError` itself lives in `src/errors/http-error.ts`), `request-id.middleware.ts`, `auth.middleware.ts` (`requireAuth`), `tenant.middleware.ts` (`resolveTenant`, `requireRole`), `platform.middleware.ts` (`requirePlatformRole`), `rate-limit.middleware.ts` (one `createRateLimiter(spec)`, reading window/limit/key-axis from `RATE_LIMITS`, `rate-limit.constants.ts`), `content-type.middleware.ts` (`requireJsonContentType`, a CSRF control — see [SECURITY.md](SECURITY.md)), `request-context.middleware.ts` (defines `requestContext`, which opens the AsyncLocalStorage store `services/request-context.service.ts` owns)                                                                                                                                        |
| `src/database/models/` | `*.model.ts`      | `user.model.ts`                       | `auth-provider.model.ts`, `audit-log.model.ts`, `email-log.model.ts` (the `email_logs` delivery log), `notification.model.ts`, `tenant-invitation.model.ts`, `tenant.model.ts`, `user-membership.model.ts`, `user-token.model.ts`, `user.model.ts` — see [DATABASE.md](DATABASE.md)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `src/utilities/`       | `*.utilities.ts`  | `response.utilities.ts`               | `response.utilities.ts` (`successResponse`, `errorResponse`, `messageResponse`), `duration.utilities.ts`, `password.utilities.ts` (`hashPassword`/`isPasswordValid`), `email-template.utilities.ts`, `origin.utilities.ts`, `cursor.utilities.ts`, `email.utilities.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `src/constants/`       | `*.constants.ts`  | `global.constants.ts`                 | `global.constants.ts`, `auth.constants.ts` (`BCRYPT_COST` and password/cookie constants — see [SECURITY.md](SECURITY.md)), `rate-limit.constants.ts` (`RATE_LIMITS`), `audit.constants.ts` (`AUDIT_ACTIONS` and their metadata schemas), `tenant.constants.ts`, `notification.constants.ts`, `queue.constants.ts`, `auth-provider.constants.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `src/configs/`         | `*.config.ts`     | `env.config.ts`                       | `env.config.ts` — the one parser of `process.env`, plus the `APP_ENV` derivation helpers — `env-consistency.config.ts` (`assertEnvConsistent`, the boot checks), `database.config.ts` (`databaseClientOptions`, the pool options), `rate-limit-store.config.ts`, `mailer.config.ts` (lazy, memoised nodemailer transporter), `cors.config.ts`, `helmet.config.ts`, and `passport.config.ts` (a deliberate pass-through verify function — the Google callback in `auth.controller.ts` calls `google-auth.service`'s `completeGoogleSignIn`, never `passport.config.ts` itself)                                                                                                                                                                                                                                         |
| `src/jobs/`            | `*.job.ts`        | `email.job.ts`                        | `email.job.ts`, `notification.job.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `src/workers/`         | `*.worker.ts`     | `email.worker.ts`                     | `email.worker.ts`, `notification.worker.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `src/templates/`       | `*.template.ts`   | `password-reset.template.ts`          | `src/templates/email/`: `email-verification.template.ts`, `password-reset.template.ts`, `password-changed.template.ts`, `registration-attempt.template.ts`, `tenant-invitation.template.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |

(`src/errors/` has no filename suffix rule — `http-error.ts`/`postgres-errors.ts`
are two of a kind, not a governed-directory pattern the way `*.service.ts`
is — called out here, as `src/observability/` is in the next table, rather
than left implied.)

Mind the asymmetry: the norm is a **singular** suffix regardless of the
directory's own name — `controller`, `repository`, `service`,
`middleware`, `model`, `config`, `policy`, `presenter`, `job`, `worker`,
`template` — but `src/validators/`, `src/utilities/`, `src/constants/` and
`src/routes/` are exceptions that keep the directory's **plural** form
(`.validators`, `.utilities`, `.constants`, `.routes`). This is exactly
what the lint rule checks — get the suffix right and don't assume it
follows the directory's own plural/singular form.

Test files (`*.test.ts`), wherever they're colocated, are exempt from this
rule — a file ending in `.test` can never also satisfy a directory's
required suffix, so `check-file` is turned off for `**/*.test.ts` and
everything under `tests/`.

## Directories with no filename rule

| Directory                  | What goes here                                                                                                                                                                                                                                                                                                                                                                                                |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/types/`               | Ambient/global type augmentation (`.d.ts` files, e.g. `express.d.ts` extending `Express.Request`) and small cross-cutting interfaces with no other natural home, e.g. `actor.ts`'s `Actor`.                                                                                                                                                                                                                   |
| `src/scripts/`             | Standalone tools run directly via `pnpm exec tsx`, not imported by the app — e.g. `generate-env-example.ts`, `platform-grant.ts`. Must live inside `src/` (not a root-level `scripts/`) so `tsconfig.json`'s `include: ["src/**/*"]` and eslint's type-aware parser can see it.                                                                                                                               |
| `src/database/migrations/` | Generated by `drizzle-kit generate` — and **committed**: CI and the production image replay migrations, they never regenerate them. Do not hand-write or hand-edit a file here beyond a documented, deliberate exception (see `0016` in [MIGRATIONS.md](MIGRATIONS.md)), and do not git-ignore them — see [DATABASE.md](DATABASE.md).                                                                         |
| `src/observability/`       | `tracing.ts` — the OpenTelemetry SDK bootstrap, loaded via `--import` before the app, after `.env` has been loaded (see `package.json`'s `dev`/`start` scripts). A single fixed-name entrypoint, not a suffix-conventioned module type — it and `console.*`/`process.env` are exempted the same way `env.config.ts`/`logger.service.ts`/`index.ts` are. See [CLAUDE.md](CLAUDE.md)'s "Observability" section. |

## Root files (no directory, no suffix rule)

`src/app.ts`, `src/server.ts`, `src/index.ts` sit directly under `src/` and
name the three stages of the boot sequence (see
[ARCHITECTURE.md](ARCHITECTURE.md)) rather than a role a suffix could
encode. There is deliberately only one of each.

## Folder naming

Every folder under `src/` must be `kebab-case`
(`check-file/folder-naming-convention`). There are no multi-word directory
names in this repo yet, so this hasn't been exercised beyond the
single-word directories above — keep it in mind the first time one is.

## No barrel files

There is no `index.ts` re-export file anywhere in `src/`, and none should
be added. Import the module directly:

Direct import — correct:

```ts
import { getEnv } from '@/configs/env.config'
```

Barrel import — avoid, and there is nothing to import it from anyway:

```ts
import { getEnv } from '@/configs'
```

A barrel would also fail `check-file/filename-naming-convention` in every
governed directory (an `index.ts` under `src/services/` cannot end in
`.service`), and it hides real edges from `import-x/no-cycle` — a cycle
routed through a barrel is invisible to that rule. See
[CLAUDE.md](CLAUDE.md) for the reasoning in one place.

## Adding a new governed directory

If a later plan introduces a new top-level concern under `src/` (e.g.
`src/jobs/` for a queue consumer), add its naming rule to
`check-file/filename-naming-convention` in `eslint.config.mjs` **and** add a
row to the "Governed directories" table above in the same change — this
file drifting from the lint rule is exactly the failure mode it exists to
prevent.
