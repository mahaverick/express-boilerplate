dist
coverage
pnpm-lock.yaml

# Generated by drizzle-kit, in drizzle-kit's own layout. Prettier would
# reformat meta/_journal.json and the generated SQL on every commit, and
# the next `drizzle-kit generate` would write it straight back — a drift
# loop between two tools that both believe they own the file.
src/database/migrations

# Untracked local scratch directories (both git-ignored). They hold
# generated plan/review artefacts, not source, and must not be able to
# fail `pnpm format:check`.
.superpowers
.worktrees
